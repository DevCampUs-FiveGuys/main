<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.MainMapperInter">
    <select id="getPortfolioDataMain" resultType="Map">
        select p.title, p.created_at, p.file_name, m.name from portfolio p join member m on p.member_id = m.member_id;
    </select>
</mapper>